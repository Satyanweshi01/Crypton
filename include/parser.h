#ifndef PARSER_H
#define PARSER_H

// Parses user input and routes commands to the appropriate module
// @param input: The user input string to be parsed
void parse_command(const char *input);

#endif
